import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#641d49',
      main: '#57103c',
      dark: '#40122f',
    },
    secondary: {
      main: '#ffffbf',
    },
  },
});

export default theme;
